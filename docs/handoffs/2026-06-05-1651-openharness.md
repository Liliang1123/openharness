# New Window Handoff: OpenHarness P5 memory/eval continuation

## 使用方式
把本文件内容粘贴到新的 Codex 窗口，并要求继续执行未完成任务。新窗口应先阅读本 handoff、项目 `AGENTS.md`、`openspec/AGENTS.md`、相关 archived change 和列出的关键文件，然后从“未完成 / 下一步”的第一个节点继续。

## 背景
- 项目路径：`/Users/elvis/file/develop/opensource/openharness`
- 当前目标：持续推进 OpenHarness Agent Runtime 能力演进，已完成 execution lifecycle、SSE replay/recovery、approval recovery、abort、terminal error、timeout、history layering、context builder、prompt registry、protocol tools、long-term memory、eval replay、memory context retrieval。
- 关键约束：
  - 默认中文交互。
  - 涉及新增能力、API 契约、架构边界、运行时语义、持久化语义、安全策略或用户可见行为变化，必须先走 OpenSpec change。
  - 严格遵循 `openspec-superpower-change` 门禁：`CONTEXT.md` checkpoint、proposal/design/tasks/spec deltas、Superpowers plan、TDD、验证、archive。
  - 重要 review 结论需落盘到 `docs/review/`；`docs/superpowers/plans/` 只保存已批准/可执行计划。
  - 不要重复执行已完成并归档的 P5a/P5b。

## 已完成
- [x] `add-execution-lifecycle-and-stream-recovery` 已完成并归档。
- [x] `add-p4a-context-builder` 已完成并归档。
- [x] `add-p4b-prompt-registry` 已完成并归档。
- [x] `add-p4c-tool-protocol` 已完成并归档。
- [x] `add-p5a-memory-and-eval` 已完成并归档。
  - 新增 `MemoryFactSchema`、`EvalCaseSchema`
  - 新增 `MemoryStore` / `InMemoryMemoryStore` / `JsonFileMemoryStore`
  - 新增 `EvalReplayHarness`
  - 新增 `docs/architecture/memory_eval_contract.md`
- [x] `add-p5b-memory-context-retrieval` 已完成并归档。
  - 新增 `memory_retrieval` ContextBuilder layer
  - `AgentExecutionRunner` 支持可选 `MemoryStore`
  - memory retrieval 使用 `(tenantId, userId, current user message)` literal search
  - memory facts 作为 system context 进入 model input，并计入 `meta.context.layers / estimatedTokens / truncated`
  - `EvalReplayHarness` 支持可选 `MemoryStore`
- [x] 全局 skill `handoff-new-window` 已创建：
  - `/Users/elvis/.codex/skills/handoff-new-window/SKILL.md`
  - 固定输出目录：`/Users/elvis/.codex/handoffs/`

## 当前状态
- Active change：无。`npx openspec list` 输出 `No active changes found.`
- 当前 specs：21 个 specs，包含 `long-term-memory`、`eval-replay`、`context-builder`、`shared-schema` 等。
- 近期 archived changes：
  - `openspec/changes/archive/2026-06-05-add-p3b-cost-and-router`
  - `openspec/changes/archive/2026-06-05-add-p3d-injection-guard`
  - `openspec/changes/archive/2026-06-05-add-p4a-context-builder`
  - `openspec/changes/archive/2026-06-05-add-p4b-prompt-registry`
  - `openspec/changes/archive/2026-06-05-add-p4c-tool-protocol`
  - `openspec/changes/archive/2026-06-05-add-p5a-memory-and-eval`
  - `openspec/changes/archive/2026-06-05-add-p5b-memory-context-retrieval`
- 服务/端口状态：
  - `lsof -nP -iTCP:8080 -sTCP:LISTEN` 无输出
  - `lsof -nP -iTCP:3001 -sTCP:LISTEN` 无输出
- Git 状态注意：
  - 父仓库 `/Users/elvis/file/develop/opensource` 仍把 `openharness` 视为未跟踪目录，所以 `git status` 不能提供 openharness 内部文件级 diff。

## 未完成 / 下一步
- [ ] 可选下一步 A：验证并迭代全局 `handoff-new-window` skill 的真实 handoff 输出质量。
  - 当前已首次生成本 handoff。
  - 若新窗口发现字段不足，再回头改 `/Users/elvis/.codex/skills/handoff-new-window/SKILL.md`。
- [ ] 可选下一步 B：继续 OpenHarness 下一轮 OpenSpec change。
  - 建议候选：`add-p5c-memory-management-api` 或 `add-p5c-eval-cli`.
  - 需先创建新的 OpenSpec proposal/design/tasks/spec deltas，再生成 `docs/superpowers/plans/YYYY-MM-DD-<change-id>.md`。
- [ ] 可选下一步 C：做 `handoff-new-window` skill 的 `agents/openai.yaml` 元数据和轻量校验脚本。
  - 当前 skill 只有 `SKILL.md`，已可用；没有 UI metadata。

## 建议下一步
- 建议先做：`add-p5c-memory-management-api` 的 OpenSpec proposal。
  - 原因：P5a/P5b 已有 MemoryStore 和 memory retrieval，但缺少 operator/dev 可调用的 memory 管理入口；做 API 能让 memory fact 创建/删除/查看进入可测试路径。
- 不建议现在做：vector DB / embeddings / semantic retrieval。
  - 原因：当前 P5b 明确 literal deterministic retrieval；直接引入 vector 会扩大依赖、测试不确定性和隐私/成本边界。
- 如果用户优先关注“新窗口 handoff 工作流”：先验证本 handoff 是否满足续跑，再补 skill 的 `agents/openai.yaml` 或脚本，而不是继续 OpenHarness 功能。

## 涉及文件
- 全局 skill：
  - `/Users/elvis/.codex/skills/handoff-new-window/SKILL.md`
  - `/Users/elvis/.codex/handoffs/2026-06-05-1651-openharness.md`
  - `/Users/elvis/.codex/handoffs/latest.md`
- 项目规则：
  - `AGENTS.md`
  - `openspec/AGENTS.md`
  - `openspec/project.md`
  - `CONTEXT.md`
- P5a/P5b 文档与计划：
  - `docs/architecture/memory_eval_contract.md`
  - `docs/superpowers/plans/2026-06-05-add-p5a-memory-and-eval.md`
  - `docs/superpowers/plans/2026-06-05-add-p5b-memory-context-retrieval.md`
  - `openspec/changes/archive/2026-06-05-add-p5a-memory-and-eval/tasks.md`
  - `openspec/changes/archive/2026-06-05-add-p5b-memory-context-retrieval/tasks.md`
- P5a/P5b runtime 关键文件：
  - `packages/shared-schema/src/index.ts`
  - `packages/shared-schema/test/schema.test.ts`
  - `agent-runtime/src/memoryStore.ts`
  - `agent-runtime/src/evalReplayHarness.ts`
  - `agent-runtime/src/contextBuilder.ts`
  - `agent-runtime/src/agentExecutionRunner.ts`
  - `agent-runtime/test/memoryStore.test.ts`
  - `agent-runtime/test/evalReplayHarness.test.ts`
  - `agent-runtime/test/contextBuilder.test.ts`
  - `agent-runtime/test/agentExecutionRunner.test.ts`
- OpenSpec current specs：
  - `openspec/specs/long-term-memory/spec.md`
  - `openspec/specs/eval-replay/spec.md`
  - `openspec/specs/context-builder/spec.md`
  - `openspec/specs/shared-schema/spec.md`

## 验证记录
- P5a 最终验证：
  - `pnpm typecheck`：通过
  - `pnpm test`：通过，shared-schema 22、agent-runtime 151、frontend 11、integration 17
  - `mvn test`：通过，26 tests，`BUILD SUCCESS`
  - `npx openspec validate --all --strict --no-interactive`：通过，21 passed / 0 failed
- P5b 最终验证：
  - targeted：`pnpm --filter @openharness/agent-runtime test -- evalReplayHarness contextBuilder agentExecutionRunner`：22/22 通过
  - `pnpm typecheck`：通过
  - `pnpm test`：通过，shared-schema 22、agent-runtime 156、frontend 11、integration 17
  - `mvn test`：通过，26 tests，`BUILD SUCCESS`
  - active `npx openspec validate add-p5b-memory-context-retrieval --strict --no-interactive`：通过
  - post-archive `npx openspec validate --all --strict --no-interactive`：通过，21 passed / 0 failed
  - `npx openspec list`：`No active changes found.`
- 本 handoff 生成前检查：
  - `npx openspec list`：`No active changes found.`
  - `npx openspec list --specs`：列出 21 个 specs
  - `lsof -nP -iTCP:8080 -sTCP:LISTEN`：无输出
  - `lsof -nP -iTCP:3001 -sTCP:LISTEN`：无输出

## 风险 / 注意事项
- OpenSpec CLI 反复出现 `edge.openspec.dev` / PostHog telemetry DNS 报错，退出码为 0；这是非阻塞网络/telemetry 问题，不影响 validate 结论。
- `pnpm test` 中 agent-runtime 的既有 stderr：
  - `[auto-compress] failed, skipping: TypeError: javaClient.request is not a function`
  - MCP malformed JSON / nonexistent server 的负向测试日志
  - 这些测试退出码为 0，不是失败。
- P5b memory retrieval 是 read-only、literal、bounded：
  - 默认 `MEMORY_CONTEXT_MAX_FACTS=5`
  - 不做 vector retrieval
  - 不做 automatic memory extraction
  - 不由 Java Backend 写 memory facts
- 若下一步新增 API/UI/语义检索，必须新建 OpenSpec change。
- 用户明确要求任务进度更新时，每个任务节点完成后要汇报：
  - 当前任务进度
  - 下一步到哪个任务节点
  - 对应任务实施建议

## 给新窗口的启动指令
继续 OpenHarness 后续任务。不要重复执行已完成并归档的 `add-p5a-memory-and-eval`、`add-p5b-memory-context-retrieval`。先阅读本 handoff、`AGENTS.md`、`openspec/AGENTS.md`、`CONTEXT.md`、`docs/architecture/memory_eval_contract.md`、相关 archived tasks 和列出的关键文件。确认 `npx openspec list` 仍无 active change 后，从“未完成 / 下一步”中选择一个新任务；若选择 OpenHarness 功能演进，必须先按 `openspec-superpower-change` 创建新 OpenSpec proposal/design/tasks/spec deltas 和 Superpowers plan，再 TDD 实施、验证、归档。每完成一个任务节点，都要向用户汇报当前进度、下一节点和实施建议。
